---
/**
 * Locations: Sección de sedes principales y distribuidores autorizados
 * Clonación Estricta: Se respeta el diseño visual idéntico al online.
 */
import Icons from "../shared/Icons.astro";

const branchDistributors = [
    { name: "Distribuidora occidente", city: "Medellín", email: "medellin@mundohomeo.co" },
    { name: "Sede valle del Cauca", city: "Cali", email: "cali@mundohomeo.co" },
    { name: "Farma costa norte", city: "Barranquilla", email: "barranquilla@mundohomeo.co" }
];
---

<section id="sedes" class="scroll-mt-10">
    <header class="mb-8 flex flex-col gap-1.5">
        <h2 class="text-[2.25rem] leading-tight font-extrabold text-secondary tracking-tight">Sedes y distribuciones</h2>
        <p class="text-slate-700 text-base font-medium">Encuentra nuestra sede clínica y puntos autorizados.</p>
    </header>

    <div class="flex flex-col gap-8">
        <!-- Tarjeta Sede Principal -->
        <div class="w-full bg-surface-white rounded-2xl border border-subtle/80 overflow-hidden flex flex-col lg:flex-row group hover:border-strong transition-all duration-500">
            <div class="flex flex-1 flex-col justify-between p-8 lg:p-12">
                <div>
                    <div class="inline-flex items-center gap-2 rounded-lg bg-brand/5 px-3 py-1 text-[11px] font-bold text-brand mb-6 border border-brand/20">
                        <!-- Icono Edificios (Hugeicons) -->
                        <Icons type="building" class="w-3.5 h-3.5" strokeWidth={2.2} />
                        Sede principal y laboratorio
                    </div>
                    <h3 class="text-secondary text-2xl font-extrabold mb-8 tracking-tight">Mundo homeopático Bogotá</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <div class="space-y-3">
                            <div class="flex items-center gap-2.5 text-brand">
                                <!-- Icono Dirección/Ubicación (Hugeicons) -->
                                <Icons type="map-pin" class="w-5 h-5" strokeWidth={2} />
                                <span class="text-[11px] font-bold text-slate-700">Dirección</span>
                            </div>
                            <p class="text-body leading-relaxed font-semibold text-[15px]">Calle de la Salud 123<br>Edificio San Lucas, Piso 4<br>Bogotá D.C.</p>
                        </div>
                        <div class="space-y-3">
                            <div class="flex items-center gap-2.5 text-brand">
                                <!-- Icono Horarios (Hugeicons) -->
                                <Icons type="clock" class="w-5 h-5" strokeWidth={2} />
                                <span class="text-[11px] font-bold text-slate-700">Horarios</span>
                            </div>
                            <p class="text-body leading-relaxed text-[15px]">
                                <span class="font-bold">Lunes a viernes:</span> 8:00 AM - 6:00 PM<br> <span class="font-bold">Sábados:</span> 9:00 AM - 1:00 PM
                            </p>
                        </div>
                    </div>
                </div>
                <div class="flex flex-col sm:flex-row gap-4 mt-10">
                    <button class="flex-1 flex items-center justify-center gap-2.5 rounded-xl bg-brand-vibrant px-6 py-4 text-surface-white font-bold hover:bg-brand transition-all focus:ring-2 focus:ring-brand focus:ring-offset-1 active:scale-95 hover:-translate-y-0.5">
                        <!-- Icono WhatsApp (Hugeicons) -->
                        <Icons type="whatsapp" class="w-5 h-5" strokeWidth={2.1} />
                        WhatsApp
                    </button>
                    <button class="flex-1 flex items-center justify-center gap-2.5 rounded-xl bg-surface-white border border-strong px-6 py-4 text-body font-bold hover:border-brand/50 hover:bg-surface-muted transition-all focus:ring-2 focus:ring-strong focus:ring-offset-1 active:scale-95">
                        <!-- Icono Teléfono (Hugeicons) -->
                        <Icons type="phone" class="w-5 h-5" strokeWidth={1.8} />
                        Llamadas
                    </button>
                </div>
            </div>
            <div class="relative w-full lg:w-[40%] min-h-[300px] lg:min-h-0 bg-surface-muted overflow-hidden border-l border-subtle/50">
                <div class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1526778548025-fa2f459cd5c1?q=80&w=800&auto=format&fit=crop')] bg-cover bg-center grayscale contrast-125 opacity-20 filter blur-[1px]"></div>
                <div class="absolute inset-0 flex items-center justify-center">
                    <div class="relative z-10 flex flex-col items-center group-hover:-translate-y-1 transition-transform duration-500">
                        <div class="bg-surface-white px-4 py-2 rounded-lg border border-subtle whitespace-nowrap mb-2 relative">
                            <p class="text-[13px] font-bold text-secondary">Sede principal MH</p>
                            <div class="absolute -bottom-1.5 left-1/2 -translate-x-1/2 w-3 h-3 bg-surface-white border-r border-b border-subtle rotate-45"></div>
                        </div>
                        <div class="relative flex items-center justify-center h-12 w-12 bg-brand rounded-full text-surface-white border-4 border-surface-white">
                            <!-- Icono Ubicación/Pin (Hugeicons) -->
                            <Icons type="map-pin" class="w-6 h-6" strokeWidth={2.2} />
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Grilla de Distribuidores -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {branchDistributors.map((distributor) => (
                <div class="bg-surface-white rounded-2xl p-6 border border-subtle hover:border-brand/30 transition-all group flex flex-col">
                    <div class="flex items-center gap-3.5 mb-5">
                        <div class="h-10 w-10 rounded-xl bg-surface-muted border border-subtle flex items-center justify-center text-brand group-hover:bg-brand group-hover:text-surface-white transition-colors duration-300">
                            <!-- Icono Edificios (Hugeicons) -->
                            <Icons type="building" class="w-5 h-5" strokeWidth={2} />
                        </div>
                        <div>
                            <h4 class="text-[15px] font-bold text-secondary leading-tight">{distributor.name}</h4>
                            <p class="text-slate-700 text-[11px] font-bold">{distributor.city}</p>
                        </div>
                    </div>
                    <div class="mt-auto space-y-3">
                        <div class="flex items-center gap-2 text-slate-700">
                            <!-- Icono Email (Hugeicons) -->
                            <Icons type="mail" class="w-3.5 h-3.5" strokeWidth={2} />
                            <span class="text-[13px] font-medium">{distributor.email}</span>
                        </div>
                        <button class="w-full flex items-center justify-center gap-2 rounded-lg bg-surface-muted border border-strong px-4 py-2.5 text-body font-bold text-[13px] hover:bg-surface-muted transition-colors">
                            <!-- Icono Teléfono (Hugeicons) -->
                            <Icons type="phone" class="w-4 h-4" strokeWidth={1.8} />
                            Llamar sucursal
                        </button>
                    </div>
                </div>
            ))}
        </div>
    </div>
</section>
