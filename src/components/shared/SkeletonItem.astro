---
interface Props {
    width?: string;
    height?: string;
    rounded?: string;
    className?: string;
}

const { width = "w-full", height = "h-4", rounded = "rounded", className = "" } = Astro.props;
---

<div class={`animate-pulse bg-surface-muted ${width} ${height} ${rounded} ${className}`}></div>
