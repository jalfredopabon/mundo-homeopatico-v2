---
// c:\Users\Alfredo Pabón\Documents\Proyectos antigravity\mundo_homeopatico_v2\src\pages\index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import SidebarCatalogo from '../components/catalogo/SidebarCatalogo.astro';
import CartDrawer from '../components/cart/CartDrawer.astro';
import Footer from '../components/shared/Footer.astro';
import Icons from '../components/shared/Icons.astro';

/**
 * Página Principal (Catálogo): 
 * Clonación Estricta del panel principal para paridad visual 1:1.
 * Se aplica el estándar "Sage Green Real" (#E7EDE3).
 */
---

<BaseLayout title="Catálogo de Productos | Mundo Homeopático" mostrarZoom={true}>
    
    <!-- Carrito Lateral (Drawer) -->
    <CartDrawer />
    
    <!-- Contenedor Principal de 2 columnas -->
    <div class="flex flex-col md:flex-row w-full flex-1 h-[calc(100vh-64px)]">
        
        <!-- Columna Izquierda: Sidebar de Filtros (Fijo en Desktop) -->
        <SidebarCatalogo />

        <!-- Columna Derecha: Contenedor Master de Data + Footer (Standard Branded Gray) -->
        <div class="flex-1 overflow-y-auto w-full relative flex flex-col bg-surface-muted h-[calc(100vh-64px)] scroll-smooth" id="catalogo-main-scroll">
            
            <!-- Banner Informativo (Cinta de Alerta Frontal) -->
            <div id="info-banner" class="w-full bg-surface-white border-b border-subtle px-4 md:px-16 py-3 sm:py-4 flex items-start justify-between gap-4 transition-all duration-300">
                <div class="flex items-start gap-4">
                    <div class="bg-brand text-white p-2 rounded-lg shrink-0">
                        <Icons type="alert-triangle" class="w-[18px] h-[18px]" strokeWidth={2.5} />
                    </div>
                    <div class="flex flex-col">
                        <span class="text-slate-900 font-bold text-[14px]">Se informa al público</span>
                        <span class="text-slate-700 text-[13px] leading-relaxed mt-0.5">Dispensación limitada a usuarios con receta médica vigente. Validación de stock: identifique si el artículo seleccionado cuenta con entrega inmediata o es de elaboración bajo pedido.</span>
                    </div>
                </div>
                <button id="close-banner" class="text-slate-700 hover:text-body hover:bg-brand/10 p-1.5 rounded-lg transition-colors shrink-0 focus:outline-none focus:ring-2 focus:ring-brand focus:ring-offset-1" aria-label="Cerrar aviso">
                    <Icons type="close-x" class="w-[18px] h-[18px]" />
                </button>
            </div>

            <div class="px-8 md:px-[72px] pt-8 pb-8 flex-1 flex flex-col">
                <div class="flex flex-col gap-8">
                    <!-- Sección: Oligoelementos -->
                    <section id="oligoelementos" class="scroll-mt-16">
                        <header class="sticky top-0 md:static z-30 bg-surface-muted -mx-8 px-8 md:-mx-[72px] md:px-[72px] pt-0 pb-8 flex flex-col auto-animate-header">
                            <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-[11px] font-bold text-slate-500 tracking-wider mb-2">
                                <span class="hover:text-slate-800 transition-colors cursor-pointer" onclick="window.location.hash='#top'">De nuestra farmacia</span>
                                <Icons type="chevron-right" class="w-2.5 h-2.5 text-slate-400" strokeWidth={3} />
                                <span class="hover:text-slate-800 transition-colors cursor-pointer">Según prescripción</span>
                            </nav>
                            <h2 class="text-[2.25rem] leading-[0.9] font-extrabold text-slate-900 tracking-tight mb-2">Oligoelementos</h2>
                            <p class="text-slate-700 text-[15px] font-medium leading-snug">Elaborados a la D7</p>
                        </header>
                        <div class="w-full bg-surface-white rounded-2xl border border-subtle overflow-hidden">
                            <!-- Encabezado de Tabla (Solo Desktop) -->
                            <div class="hidden sm:grid grid-cols-[1fr_120px_120px_70px] md:grid-cols-[1fr_130px_130px_100px] gap-4 items-center bg-surface-muted/50 border-b border-subtle py-3.5 px-6 text-[12px] font-bold text-slate-700 tracking-wide">
                                <div class="invisible">Producto</div>
                                <div class="text-right">Precio farmacia</div>
                                <div class="text-right">Precio público</div>
                                <div class="flex justify-center">
                                    <Icons type="cart" class="w-4 h-4 text-slate-700" strokeWidth={2.3} />
                                </div>
                            </div>

                            <!-- Fila de Producto 1 -->
                            <div class="flex flex-col sm:grid sm:grid-cols-[1fr_120px_120px_70px] md:grid-cols-[1fr_130px_130px_100px] gap-2 sm:gap-4 sm:items-center py-5 px-6 border-b border-subtle cursor-pointer hover:bg-surface-muted transition-all relative z-0 hover:z-10 group">
                                <div class="flex flex-col pl-0">
                                    <h3 class="text-slate-900 font-bold text-base md:text-[1.05rem] leading-tight transition-colors">Oligoelementos individuales</h3>
                                    <div class="flex flex-wrap items-center gap-2 mt-2">
                                        <span class="inline-flex items-center text-[10px] font-bold text-slate-700 bg-surface-muted group-hover:bg-surface-white group-hover:border-slate-300 transition-colors px-2.5 py-1 rounded-md border border-subtle">
                                            Gotas / Spray
                                        </span>
                                        <span class="inline-flex items-center text-[10px] font-bold text-slate-700 bg-surface-muted group-hover:bg-surface-white group-hover:border-slate-300 transition-colors px-2.5 py-1 rounded-md border border-subtle">
                                            Glóbulos x 30g
                                        </span>
                                    </div>
                                </div>
                                <div class="sm:contents">
                                    <div class="flex flex-col sm:block text-left sm:text-right mt-3 sm:mt-0 pt-3 sm:pt-0 border-t border-subtle/30 sm:border-0">
                                        <span class="sm:hidden text-[10px] text-slate-500 font-bold uppercase tracking-wider mb-0.5">Farmacia</span>
                                        <span class="text-slate-700 font-medium">$19.500</span>
                                    </div>
                                    <div class="hidden sm:block text-right mt-1 sm:mt-0">
                                        <span class="text-slate-700 font-medium">$36.000</span>
                                    </div>
                                    <div class="flex justify-end sm:justify-center mt-3 sm:mt-0">
                                        <button class="w-full sm:w-10 h-10 flex items-center justify-center rounded-lg bg-emerald-700 hover:bg-brand text-white hover:-translate-y-0.5 transition-all focus:ring-2 focus:ring-brand active:scale-95 btn-add-cart" data-name="Oligoelementos Individuales" data-price="19500" aria-label="Añadir Oligoelementos Individuales">
                                            <div class="flex items-center justify-center mt-[-1px]">
                                                <Icons type="plus" class="w-[18px] h-[18px]" strokeWidth={2.5} />
                                                <span class="sm:hidden ml-2 font-bold text-sm leading-none">Añadir al pedido</span>
                                            </div>
                                        </button>
                                    </div>
                                </div>
                            </div>

                            <!-- Fila de Producto 2 -->
                            <div class="flex flex-col sm:grid sm:grid-cols-[1fr_120px_120px_70px] md:grid-cols-[1fr_130px_130px_100px] gap-2 sm:gap-4 sm:items-center py-5 px-6 border-b border-subtle cursor-pointer hover:bg-surface-muted transition-all relative z-0 hover:z-10 group">
                                <div class="flex flex-col pl-0">
                                    <h3 class="text-slate-900 font-bold text-base md:text-[1.05rem] leading-tight transition-colors">Oligoelementos combinados (hasta 6 componentes)</h3>
                                    <div class="flex flex-wrap items-center gap-2 mt-2">
                                        <span class="inline-flex items-center text-[10px] font-bold text-slate-700 bg-surface-muted group-hover:bg-surface-white group-hover:border-slate-300 transition-colors px-2.5 py-1 rounded-md border border-subtle">
                                            Gotas / Spray
                                        </span>
                                        <span class="inline-flex items-center text-[10px] font-bold text-slate-700 bg-surface-muted group-hover:bg-surface-white group-hover:border-slate-300 transition-colors px-2.5 py-1 rounded-md border border-subtle">
                                            Glóbulos x 30g
                                        </span>
                                    </div>
                                </div>
                                <div class="sm:contents">
                                    <div class="flex flex-col sm:block text-left sm:text-right mt-3 sm:mt-0 pt-3 sm:pt-0 border-t border-subtle/30 sm:border-0">
                                        <span class="sm:hidden text-[10px] text-slate-500 font-bold uppercase tracking-wider mb-0.5">Farmacia</span>
                                        <span class="text-slate-700 font-medium">$25.500</span>
                                    </div>
                                    <div class="hidden sm:block text-right mt-1 sm:mt-0">
                                        <span class="text-slate-700 font-medium">$44.000</span>
                                    </div>
                                    <div class="flex justify-end sm:justify-center mt-3 sm:mt-0">
                                        <button class="w-full sm:w-10 h-10 flex items-center justify-center rounded-lg bg-emerald-700 hover:bg-brand text-white hover:-translate-y-0.5 transition-all focus:ring-2 focus:ring-brand active:scale-95 btn-add-cart" data-name="Oligoelementos combinados" data-price="25500" aria-label="Añadir Oligoelementos combinados">
                                            <div class="flex items-center justify-center mt-[-1px]">
                                                <Icons type="plus" class="w-[18px] h-[18px]" strokeWidth={2.5} />
                                                <span class="sm:hidden ml-2 font-bold text-sm leading-none">Añadir al pedido</span>
                                            </div>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </section>
                    
                    <!-- Placeholder: Esencias florales -->
                    <section id="esencias-florales" class="scroll-mt-16">
                        <header class="sticky top-0 md:static z-30 bg-surface-muted -mx-8 px-8 md:-mx-[72px] md:px-[72px] pt-0 pb-8 flex flex-col">
                            <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-[11px] font-bold text-slate-500 tracking-wider mb-2">
                                <span>De nuestra farmacia</span>
                                <Icons type="chevron-right" class="w-2.5 h-2.5 text-slate-400" strokeWidth={3} />
                                <span>Según prescripción</span>
                            </nav>
                            <h2 class="text-[2.25rem] leading-[0.9] font-extrabold text-slate-900 tracking-tight mb-2">Esencias florales</h2>
                            <p class="text-slate-700 text-[15px] font-medium leading-snug">Flores de Bach y otras esencias</p>
                        </header>
                        <div class="w-full bg-surface-white rounded-2xl border border-subtle overflow-hidden p-8 flex items-center justify-center min-h-[200px]">
                            <span class="text-slate-700 text-sm font-medium">Tabla de esencias florales (datos reales en Fase 3)</span>
                        </div>
                    </section>

                    <!-- Placeholder: Alimentos funcionales -->
                    <section id="alimentos-funcionales" class="scroll-mt-16">
                        <header class="sticky top-0 md:static z-30 bg-surface-muted -mx-8 px-8 md:-mx-[72px] md:px-[72px] pt-0 pb-8 flex flex-col">
                            <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-[11px] font-bold text-slate-500 tracking-wider mb-2">
                                <span>Productos exclusivos</span>
                            </nav>
                            <h2 class="text-[2.25rem] leading-[0.9] font-extrabold text-slate-900 tracking-tight mb-2">Alimentos funcionales</h2>
                            <p class="text-slate-700 text-[15px] font-medium leading-snug">Suplementos y alimentos de alta calidad</p>
                        </header>
                        <div class="w-full bg-surface-white rounded-2xl border border-subtle overflow-hidden p-8 flex items-center justify-center min-h-[200px]">
                            <span class="text-slate-700 text-sm font-medium">Tabla de alimentos funcionales (datos reales en Fase 3)</span>
                        </div>
                    </section>

                    <!-- Placeholder: CBD -->
                    <section id="cbd" class="scroll-mt-16">
                        <header class="sticky top-0 md:static z-30 bg-surface-muted -mx-8 px-8 md:-mx-[72px] md:px-[72px] pt-0 pb-8 flex flex-col">
                            <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-[11px] font-bold text-slate-500 tracking-wider mb-2">
                                <span>Productos exclusivos</span>
                            </nav>
                            <h2 class="text-[2.25rem] leading-[0.9] font-extrabold text-slate-900 tracking-tight mb-2">CBD</h2>
                            <p class="text-slate-700 text-[15px] font-medium leading-snug">Productos de cannabidiol de grado farmacéutico</p>
                        </header>
                        <div class="w-full bg-surface-white rounded-2xl border border-subtle overflow-hidden p-8 flex items-center justify-center min-h-[200px]">
                            <span class="text-slate-700 text-sm font-medium">Tabla de CBD (datos reales en Fase 3)</span>
                        </div>
                    </section>

                    <!-- Placeholder: Aceites esenciales -->
                    <section id="aceites-esenciales" class="scroll-mt-16">
                        <header class="sticky top-0 md:static z-30 bg-surface-muted -mx-8 px-8 md:-mx-[72px] md:px-[72px] pt-0 pb-8 flex flex-col">
                            <nav aria-label="Breadcrumb" class="flex items-center gap-2 text-[11px] font-bold text-slate-500 tracking-wider mb-2">
                                <span>Productos exclusivos</span>
                            </nav>
                            <h2 class="text-[2.25rem] leading-[0.9] font-extrabold text-slate-900 tracking-tight mb-2">Aceites esenciales</h2>
                            <p class="text-slate-700 text-[15px] font-medium leading-snug">Extractos puros de plantas medicinales</p>
                        </header>
                        <div class="w-full bg-surface-white rounded-2xl border border-subtle overflow-hidden p-8 flex items-center justify-center min-h-[200px]">
                            <span class="text-slate-700 text-sm font-medium">Tabla de aceites esenciales (datos reales en Fase 3)</span>
                        </div>
                    </section>
                </div>
            </div>

            <!-- Footer corporativo unificado -->
            <Footer />
        </div>

        <!-- Botón Back To Top -->
        <button id="back-to-top" class="fixed bottom-8 right-8 w-11 h-11 bg-white text-slate-800 rounded-full flex items-center justify-center shadow-xl translate-y-20 opacity-0 invisible hover:-translate-y-1 hover:text-brand transition-all duration-500 z-50 ring-1 ring-slate-100 focus:outline-none focus:ring-2 focus:ring-brand" aria-label="Volver arriba">
            <Icons type="arrow-up" class="w-5 h-5" />
        </button>

    </div>
</BaseLayout>

<script>
    document.addEventListener('astro:page-load', () => {
        // --- 1. Lógica del Carrito con Micro-animaciones ---
        const btnAddCart = document.querySelectorAll('.btn-add-cart');
        
        btnAddCart.forEach(btn => {
            const button = btn as HTMLButtonElement;
            button.addEventListener('click', () => {
                const name = button.getAttribute('data-name');
                const price = button.getAttribute('data-price');

                // Lanzar evento para CartDrawer.astro
                document.dispatchEvent(new CustomEvent('addToCart', {
                    detail: { name, price }
                }));

                // Micro-animación: Mutación del botón (+ → ✓)
                const originalContent = button.innerHTML;
                button.innerHTML = '<div class="flex items-center justify-center shadow-sm"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg></div>';
                button.classList.add('bg-brand', 'scale-110');
                
                setTimeout(() => {
                    button.innerHTML = originalContent;
                    button.classList.remove('bg-brand', 'scale-110');
                }, 1200);
            });
        });

        // --- 2. Cierre del Banner ---
        const btnCloseBanner = document.getElementById('close-banner');
        const infoBanner = document.getElementById('info-banner');
        
        if (btnCloseBanner && infoBanner) {
            btnCloseBanner.addEventListener('click', () => {
                infoBanner.style.opacity = '0';
                infoBanner.style.transform = 'translateY(-10px)';
                setTimeout(() => {
                    infoBanner.style.display = 'none';
                }, 300);
            });
        }

        // --- 3. Back To Top Dinámico ---
        const btnBackToTop = document.getElementById('back-to-top');
        const scrollContainer = document.getElementById('catalogo-main-scroll');

        if (btnBackToTop && scrollContainer) {
            scrollContainer.addEventListener('scroll', () => {
                if (scrollContainer.scrollTop > 400) {
                    btnBackToTop.classList.remove('opacity-0', 'invisible', 'translate-y-20');
                } else {
                    btnBackToTop.classList.add('opacity-0', 'invisible', 'translate-y-20');
                }
            });

            btnBackToTop.addEventListener('click', () => {
                scrollContainer.scrollTo({ top: 0, behavior: 'smooth' });
            });
        }
    });
</script>
